{% extends 'base.html.twig' %}

{% block title %}Pessoa{% endblock %}

{% block body %}
    <h1>Pessoa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pessoa.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ pessoa.tipo }}</td>
            </tr>
            <tr>
                <th>Identificador</th>
                <td>{{ pessoa.identificador }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ pessoa.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pessoa_index') }}">back to list</a>

    <a href="{{ path('pessoa_edit', {'id': pessoa.id}) }}">edit</a>

    {{ include('pessoa/_delete_form.html.twig') }}
{% endblock %}
